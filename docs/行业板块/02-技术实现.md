# 行业板块 - 技术实现

> 📚 **相关文档**: [使用指南](./01-使用指南.md) | [返回上级](./README.md)

本文档面向**开发者**，详细介绍行业板块页面的技术架构、实现细节和扩展方法。

---

## 🏗️ 整体架构

### 数据流图

```
用户访问 IndustrySectorPage
    ↓
useEffect (首次加载)
    ↓
调用 getUnifiedIndustryBasic() - 加载所有行业基本信息（缓存）
    ↓
调用 getUnifiedIndustryRank() - 加载当前页数据
    ↓
数据解析 & 格式化
    ↓
更新本地状态 (data, total, loading, allSectors)
    ↓
UI 组件渲染
    ├── AutoComplete 搜索框
    ├── 排序选择器
    ├── 数据表格
    └── 分页控件

用户输入搜索关键词
    ↓
防抖处理 (300ms)
    ↓
过滤 allSectors 显示匹配建议
    ↓
用户选择板块
    ↓
切换到详情模式 (viewMode = 'detail')
    ↓
调用 getSingleIndustrySector() 获取详细数据
    ↓
显示单个板块信息，停止自动刷新

点击板块名称
    ↓
打开 IndustrySectorStocksDrawer
    ↓
调用 getIndustrySectorStocks() API
    ↓
渲染成分股列表

usePolling Hook (每10秒)
    ↓
检查 viewMode === 'list'
    ↓
是：静默刷新当前页数据
    ↓
否：跳过刷新（详情模式）
```

### 核心组件

| 组件         | 文件路径                                                                   | 职责                                   |
| ------------ | -------------------------------------------------------------------------- | -------------------------------------- |
| **主页面**   | `src/pages/IndustrySectorPage/IndustrySectorPage.tsx`                      | 页面布局、数据加载、状态管理、视图切换 |
| **抽屉组件** | `src/components/IndustrySectorStocksDrawer/IndustrySectorStocksDrawer.tsx` | 成分股展示                             |
| **统一服务** | `src/services/hot/unified-sectors.ts`                                      | 统一缓存服务，提供行业板块数据         |

---

## 💾 数据存储

### 状态管理

**文件**: `src/pages/IndustrySectorPage/IndustrySectorPage.tsx`

**状态定义**:

```typescript
const [searchKeyword, setSearchKeyword] = useState('');
const [sortOrder, setSortOrder] = useState<number>(1); // 1=降序, 0=升序
const [data, setData] = useState<IndustrySectorRankData[]>([]);
const [total, setTotal] = useState(0);
const [loading, setLoading] = useState(false);
const [currentPage, setCurrentPage] = useState(1);

// 视图模式：'list' - 行业列表，'detail' - 成分股详情
const [viewMode, setViewMode] = useState<'list' | 'detail'>('list');
const [selectedSector, setSelectedSector] = useState<{ code: string; name: string } | null>(null);

// 所有行业分类缓存数据（用于搜索）
const [allSectors, setAllSectors] = useState<IndustrySectorBasicInfo[]>([]);

// 控制 AutoComplete 下拉框显示
const [dropdownOpen, setDropdownOpen] = useState(false);

// 行业详情抽屉状态
const [drawerOpen, setDrawerOpen] = useState(false);
const [drawerSector, setDrawerSector] = useState<{ code: string; name: string } | null>(null);

// 防止重复请求
const isFetchingRef = useRef(false);

// 搜索防抖定时器
const searchTimerRef = useRef<NodeJS.Timeout | null>(null);
```

### 类型定义

**文件**: `src/types/stock.ts`

```typescript
export interface IndustrySectorRankData {
  code: string; // 板块代码
  name: string; // 板块名称
  changePercent: number; // 涨跌幅（百分比）

  // 主力净流入
  mainNetInflow?: number; // 净额（万元）
  mainNetInflowRatio?: number; // 净占比（百分比）

  // 超大单净流入
  superLargeNetInflow?: number; // 净额（万元）
  superLargeNetInflowRatio?: number; // 净占比（百分比）

  // 大单净流入
  largeNetInflow?: number; // 净额（万元）
  largeNetInflowRatio?: number; // 净占比（百分比）

  // 中单净流入
  mediumNetInflow?: number; // 净额（万元）
  mediumNetInflowRatio?: number; // 净占比（百分比）

  // 小单净流入
  smallNetInflow?: number; // 净额（万元）
  smallNetInflowRatio?: number; // 净占比（百分比）

  // 领涨股
  leadingStock?: string; // 领涨股名称
  leadingStockCode?: string; // 领涨股代码
}
```

---

## ⚙️ 核心逻辑

### 1. 数据服务层

**统一缓存服务** (`src/services/hot/unified-sectors.ts`):

```typescript
// 获取行业板块列表（使用统一缓存服务）
export async function getUnifiedIndustryRank(
  sortOrder: number = 1,
  pageSize: number = 50,
  pageNum: number = 1
): Promise<{ data: IndustrySectorRankData[]; total: number }> {
  // 内部调用 getIndustrySectors()，但增加了缓存机制
  // 缓存键：unified_industry_rank_v1
  // 缓存时间：10秒
}

// 获取所有行业基本信息（用于搜索）
export async function getUnifiedIndustryBasic(): Promise<IndustrySectorBasicInfo[]> {
  // 从缓存或 API 获取所有行业板块的基本信息
  // 缓存键：unified_industry_basic_v1
  // 缓存时间：24小时
}
```

**原始 API 服务** (`src/services/hot/industry-sectors.ts`):

```typescript
// 获取行业板块列表
export async function getIndustrySectors(
  sortType: string = 'f3',
  sortOrder: number = 1,
  pageSize: number = 50,
  pageNum: number = 1
): Promise<{ data: IndustrySectorRankData[]; total: number }> {
  const params = new URLSearchParams({
    cb: `jQuery${Date.now()}_${Math.random().toString().slice(2, 11)}`,
    fid: sortType,
    po: sortOrder.toString(),
    pz: pageSize.toString(),
    pn: pageNum.toString(),
    np: '1',
    fltt: '2',
    invt: '2',
    ut: '8dec03ba335b81bf4ebdf7b29ec27d15',
    fs: 'm:90+s:4', // m:90 s:4 表示行业板块
    fields: 'f12,f14,f2,f3,f62,f184,f66,f69,f72,f75,f78,f81,f84,f87,f204,f205,f124,f1,f13',
  });

  const data = await getEastMoneyClistJsonpData(params, 3);
  return parseIndustrySectorData(data);
}

// 获取指定行业板块的成分股
export async function getIndustrySectorStocks(
  sectorCode: string,
  sortType: string = 'f3',
  sortOrder: number = 1,
  pageSize: number = 50,
  pageNum: number = 1,
  signal?: AbortSignal
): Promise<{ data: IndustrySectorRankData[]; total: number }> {
  const params = new URLSearchParams({
    // ... 类似参数
    fs: `b:${sectorCode}`, // b:板块代码
  });

  const data = await getEastMoneyClistJsonpData(params, 3, signal);
  return parseIndustrySectorData(data);
}

// 获取单个行业详细数据
export async function getSingleIndustrySector(
  sectorCode: string
): Promise<IndustrySectorRankData | null> {
  // 调用 /api/eastmoney/ulist.np/get 接口
  const params = new URLSearchParams({
    fltt: '2',
    secids: `90.${sectorCode}`,
    fields: 'f62,f184,f66,f69,f72,f75,f78,f81,f84,f87,f124,f6,f12,f14',
    ut: 'b2884a393a59ad64002292a3e90d46a5',
  });

  const response = await fetch(`/api/eastmoney/ulist.np/get?${params}`);
  const data = await response.json();
  // 解析并返回单个行业数据
}
```

### 2. 数据解析

```typescript
function parseIndustrySectorData(
  rawList: any[],
  total: number
): { data: IndustrySectorRankData[]; total: number } {
  const data: IndustrySectorRankData[] = rawList.map((item) => ({
    code: item.f12,
    name: item.f14,
    changePercent: item.f3,

    // 主力净流入（元转万元）
    mainNetInflow: item.f62 ? item.f62 / 10000 : undefined,
    mainNetInflowRatio: item.f184,

    // 超大单净流入（元转万元）
    superLargeNetInflow: item.f66 ? item.f66 / 10000 : undefined,
    superLargeNetInflowRatio: item.f69,

    // 大单净流入（元转万元）
    largeNetInflow: item.f72 ? item.f72 / 10000 : undefined,
    largeNetInflowRatio: item.f75,

    // 中单净流入（元转万元）
    mediumNetInflow: item.f78 ? item.f78 / 10000 : undefined,
    mediumNetInflowRatio: item.f81,

    // 小单净流入（元转万元）
    smallNetInflow: item.f84 ? item.f84 / 10000 : undefined,
    smallNetInflowRatio: item.f87,

    // 领涨股
    leadingStock: item.f204,
    leadingStockCode: item.f205,
  }));

  return { data, total };
}
```

### 3. 视图模式切换

```typescript
// 处理行业选择 - 切换到行业详情模式
const handleSectorSelect = async (sectorCode: string) => {
  const sector = allSectors.find((s) => s.code === sectorCode);
  if (!sector) {
    message.warning('未找到该行业信息');
    return;
  }

  // 切换到详情模式
  setViewMode('detail');
  setSelectedSector({ code: sector.code, name: sector.name });
  setSearchKeyword(''); // 清空搜索框
  setCurrentPage(1);
  setLoading(true);

  try {
    // 获取该行业的详细数据（资金流向）
    const data = await getSingleIndustrySector(sector.code);
    if (data) {
      setData([data]);
      setTotal(1);
    } else {
      message.warning('未获取到行业详情数据');
    }
  } catch (error) {
    logger.error('加载行业详细数据失败:', error);
    message.error('加载行业详情失败');
  } finally {
    setLoading(false);
  }
};

// 返回列表模式
const handleBackToList = () => {
  setViewMode('list');
  setSelectedSector(null);
  setCurrentPage(1);
  loadData(1);
  message.info('已返回行业列表');
};
```

### 4. AutoComplete 智能搜索

```typescript
// 搜索选项（基于本地缓存数据）
const searchOptions = useMemo(() => {
  if (allSectors.length === 0) return [];

  // 如果没有搜索关键词，显示所有行业（最多50条）
  if (!searchKeyword) {
    return allSectors.slice(0, 50).map((sector) => ({
      value: sector.code,
      label: `${sector.name} (${sector.code})`,
    }));
  }

  // 有搜索关键词时，过滤显示匹配结果
  const keyword = searchKeyword.toLowerCase();
  return allSectors
    .filter(
      (sector) =>
        sector.name.toLowerCase().includes(keyword) || sector.code.toLowerCase().includes(keyword)
    )
    .slice(0, 50) // 最多显示50条
    .map((sector) => ({
      value: sector.code,
      label: `${sector.name} (${sector.code})`,
    }));
}, [searchKeyword, allSectors]);

// 处理搜索关键词变化（带防抖）
const handleSearchChange = (value: string) => {
  setSearchKeyword(value);

  // 清除之前的定时器
  if (searchTimerRef.current) {
    clearTimeout(searchTimerRef.current);
  }

  // 设置新的定时器，300ms 后执行搜索
  searchTimerRef.current = setTimeout(() => {
    // 防抖处理完成，AutoComplete 会自动过滤
  }, SEARCH_DEBOUNCE_DELAY); // 300ms
};
```

### 5. 智能刷新机制

```typescript
// 使用标准轮询Hook - 每10秒静默刷新当前页（使用统一缓存服务）
usePolling(
  async () => {
    if (viewMode === 'list') {
      try {
        const result = await getUnifiedIndustryRank(sortOrder, pageSize, currentPage);
        setData(result.data);
        setTotal(result.total);
      } catch (error) {
        logger.error('自动刷新失败:', error);
        message.error('数据刷新失败');
      }
    }
    // 注意：在详情模式下不自动刷新，避免覆盖用户选择的数据
  },
  {
    interval: SECTOR_POLLING_INTERVAL, // 10秒
    immediate: false, // 不立即执行，因为已有初始加载
    enabled: viewMode === 'list', // 只在列表模式下启用
  }
);
```

### 6. 防重复请求机制

```typescript
// 防止重复请求
const isFetchingRef = useRef(false);

// 加载数据（使用统一缓存服务）
const loadData = useCallback(
  async (page: number = 1, silent: boolean = false) => {
    if (isFetchingRef.current) return; // 如果正在加载，直接返回

    isFetchingRef.current = true;
    if (!silent) {
      setLoading(true);
    }

    try {
      const result = await getUnifiedIndustryRank(sortOrder, pageSize, page);
      setData(result.data);
      setTotal(result.total);
      setCurrentPage(page);
    } catch (error) {
      logger.error('加载行业板块数据失败:', error);
      message.error('加载数据失败，请重试');
    } finally {
      if (!silent) {
        setLoading(false);
      }
      isFetchingRef.current = false;
    }
  },
  [sortOrder]
);
```

---

## 🚀 性能优化

### 1. 静默刷新

```typescript
// 静默刷新不显示loading状态
async function loadData(silent = false) {
  if (!silent) setLoading(true);
  // ... 数据加载
  if (!silent) setLoading(false);
}
```

### 2. 金额格式化

```typescript
const formatAmount = (value?: number): string => {
  if (!value) return '-';
  const absValue = Math.abs(value);
  if (absValue >= 10000) {
    // 大于等于1亿，显示为亿
    const yiValue = value / 10000;
    return `${yiValue.toFixed(2)}亿`;
  }
  // 否则显示为万元
  return `${value.toFixed(2)}万`;
};
```

### 3. React.memo 优化

```typescript
// 防止不必要的重渲染
const MemoizedTable = React.memo(Table);
```

---

## 🌐 API 集成

### 1. 代理配置

**开发环境** (`vite.config.ts`):

```javascript
server: {
  proxy: {
    '/api/eastmoney': {
      target: 'https://push2.eastmoney.com',
      changeOrigin: true,
      headers: {
        Referer: env.VITE_EASTMONEY_REFERER,
        Origin: env.VITE_EASTMONEY_ORIGIN,
      },
    }
  }
}
```

### 2. 请求参数详解

**列表查询参数**:

```javascript
{
  fs: 'm:90+s:4',              // 行业板块标识
  fid: 'f3',                   // 排序字段：f3=涨跌幅
  po: '1',                     // 排序方向：1=降序, 0=升序
  pz: '50',                    // 每页数量
  pn: '1',                     // 页码
  fields: 'f12,f14,f2,f3,f62,f184,f66,f69,f72,f75,f78,f81,f84,f87,f204,f205,f124,f1,f13'
}
```

**数据字段映射**:

| 字段代码 | 含义             | 说明                    |
| -------- | ---------------- | ----------------------- |
| f12      | 板块代码         | 如 BK1020               |
| f14      | 板块名称         | 如 "非金属材料 Ⅱ"       |
| f3       | 涨跌幅           | 百分比                  |
| f62      | 主力净流入净额   | 单位:元(前端转换为万元) |
| f184     | 主力净流入净占比 | 百分比                  |
| f66/f69  | 超大单净额/占比  | 同上                    |
| f72/f75  | 大单净额/占比    | 同上                    |
| f78/f81  | 中单净额/占比    | 同上                    |
| f84/f87  | 小单净额/占比    | 同上                    |
| f204     | 领涨股名称       | -                       |
| f205     | 领涨股代码       | -                       |

---

## 🎨 样式设计

### 颜色方案

**涨跌颜色**:

```css
/* 流入/上涨 */
.inflow-positive {
  color: var(--ant-color-success); /* 绿色 */
}

/* 流出/下跌 */
.outflow-negative {
  color: var(--ant-color-error); /* 红色 */
}

/* 中性数据 */
.neutral {
  color: var(--ant-color-text);
}
```

### 固定列样式

```css
.fixedLeftColumn {
  position: sticky;
  left: 0;
  z-index: 1;
  background: var(--ant-color-bg-container);
}
```

---

## 🔌 扩展方法

### 添加新的排序字段

**步骤 1**: 修改 API 请求参数

```typescript
// 添加成交额排序
const result = await getIndustrySectors('f6', sortOrder, 50, currentPage);
// f6 = 成交额
```

**步骤 2**: 更新表格列定义

```typescript
const columns = [
  // ... 现有列
  {
    title: '成交额',
    dataIndex: 'turnover',
    key: 'turnover',
    sorter: true,
  },
];
```

### 添加新的资金档位

**步骤 1**: 更新类型定义

```typescript
interface IndustrySectorRankData {
  // ... 现有字段
  retailNetInflow?: number; // 散户净流入
  retailNetInflowRatio?: number; // 散户净占比
}
```

**步骤 2**: 更新数据解析

```typescript
retailNetInflow: item.fXXX ? item.fXXX / 10000 : undefined,
retailNetInflowRatio: item.fYYY,
```

**步骤 3**: 添加表格列

```typescript
{
  title: '散户净流入',
  dataIndex: 'retailNetInflow',
  key: 'retailNetInflow',
  render: (value) => formatAmount(value),
}
```

---

## 📖 相关文件索引

| 文件                                                                       | 说明             |
| -------------------------------------------------------------------------- | ---------------- |
| `src/pages/IndustrySectorPage/IndustrySectorPage.tsx`                      | 主页面组件       |
| `src/components/IndustrySectorStocksDrawer/IndustrySectorStocksDrawer.tsx` | 成分股抽屉       |
| `src/services/hot/industry-sectors.ts`                                     | 数据服务层       |
| `src/types/stock.ts`                                                       | 类型定义         |
| `vite.config.ts`                                                           | 开发环境代理配置 |
| `electron/localApiProxy.ts`                                                | 生产环境代理配置 |

---

## 📝 更新记录

- **2026-05-09**：功能升级，新增视图模式和智能搜索

  - 更新数据流图，增加视图模式切换流程
  - 新增统一缓存服务说明（unified-sectors）
  - 补充 AutoComplete 智能搜索实现细节
  - 更新状态管理，增加 viewMode、allSectors 等状态
  - 新增视图模式切换、AutoComplete 搜索、智能刷新机制代码示例
  - 补充防重复请求机制（isFetchingRef）
  - 更新 API 集成，说明 getEastMoneyClistJsonpData 的使用

- **2026-04-19**：从“功能完整说明”拆分出技术实现文档

  - 整合 API 集成细节
  - 补充类型定义
  - 添加扩展方法说明

- **2026-04-17**：初始版本
  - 新增完整功能说明文档
  - 增加成分股查看功能说明
  - 更新与概念板块的对比
